# XINGXU FAN 网站开发计划

状态：阶段 0～3 已完成并由用户验收；网站已通过正式域名公开运行，GitHub／Workers Builds CI 已连接并通过实际构建验证。阶段 4 本地实现与生产部署已完成，但 RFQ 在邮件发送步骤失败，真实 Gmail 收件尚未验收；当前优先发布安全诊断补丁并查明明确根因，不标记阶段 4 完成
项目：Quanzhou Xingxu Fan & Ventilation Supply
目标仓库：`https://github.com/ChenYebin-new/xing-xu-website`
目标市场：Malaysia and the Philippines
首版语言：English
更新时间：2026-09-15（外部状态依据用户交接与本任务前序只读核验，本次 Git 发布不修改控制台配置）

## 1. 已锁定的开发依据

- 这是完全独立的新网站，不复制或继承旧网站的代码、布局、文字和视觉系统。
- 首页采用 `Evidence-led Showroom`（B）作为基础构图：经营身份与约三十年经验、主产品视觉、门店／仓库／产品／铭牌证据共同进入首屏。
- 吸收 `Buyer Path + RFQ Rail`（C）的询价设计，但不取代首屏的经营证据：客户可以按身份和产品类别进入询价，并提交数量、参数和目的地。
- 对外身份是供应商和采购合作伙伴，不使用 `Manufacturer`、`Factory`、虚构认证、虚构出口记录或未经确认的性能数据。
- 第一版优先服务经销商，其次是工程承包商和设备制造商。

## 2. 第一版交付范围

### 必须完成的页面

1. `/`：Home。
2. `/products/`：产品分类入口。
3. `/products/negative-pressure-fans/`。
4. `/products/axial-fans/`。
5. `/products/centrifugal-fans/`。
6. `/partners/distributors/`：首要合作页面。
7. `/partners/contractors/`。
8. `/partners/manufacturers/`。
9. `/about/`：经营经历与真实证据。
10. `/contact/`：已授权公开的联系方式。
11. `/request-a-quote/`：完整 RFQ 表单。
12. `/privacy/`、`/thank-you/` 和自定义 404 页面。

### 有内容才上线的页面

- `/applications/`：至少有一组经过核实的应用说明后再进入主导航。
- `/resources/`：至少有一份真实目录、尺寸图或技术资料后再进入主导航。
- 单个产品详情页：只有真实型号、照片和基本参数达到发布清单后才创建。

不在第一版加入账号系统、购物车、在线支付、实时库存、多语言后台或 CMS。

## 3. 技术方案

- 前端：Astro + TypeScript，静态内容优先，尽量减少浏览器端 JavaScript。
- 样式：项目内设计变量与可复用组件；不引入通用模板主题。
- 产品资料：使用经过类型校验的本地结构化内容；产品数量可逐步增加，不需要重做页面。
- 托管：Cloudflare Workers + Static Assets。
- 动态接口：同一 Cloudflare Worker 仅处理 `/api/rfq` 等必要接口，其余请求优先由静态资源服务。
- 防垃圾：Cloudflare Turnstile，服务器端调用 Siteverify 验证；前端控件本身不视为验证成功。
- 询盘通知：优先使用 Cloudflare Email Service 的 Worker 邮件绑定，将询盘发送到已验证 Gmail。生产发送需要先完成域名与发件身份设置，并做真实投递测试。
- 数据策略：第一版默认不建立询盘数据库；成功发送邮件后不额外保存完整客户资料。运行日志不得记录完整联系方式和询盘正文。
- 文件上传：第一版默认不提供；如后续确认需要图纸上传，再单独设计文件类型、大小、病毒风险、保存期限与访问权限。

依赖版本在正式开发开始时选择当日稳定版本并写入锁文件，本计划不提前锁定可能过时的版本号。

## 4. 分阶段实施

### 阶段 0：安全接入新仓库

任务：

- 保留当前目录中的规划材料和视觉稿。
- 由于远端仓库已确认为空，在当前目录初始化 `main` 分支并添加 `origin`。
- 建立只针对本项目的忽略规则，排除依赖、构建产物、环境变量和本地临时文件。
- 首次提交只包含经过确认的规划材料、视觉依据和项目骨架；不混入旧网站文件。

验收：

- 本地仓库路径、远端地址和 `main` 分支全部明确。
- 没有密钥、个人缓存、依赖目录或旧网站文件进入暂存区。
- 推送前必须先向用户展示提交范围；本计划的确认不等于自动授权推送。

### 阶段 1：建立项目骨架与内容模型

任务：

- 创建 Astro + TypeScript 项目和 Cloudflare 本地配置。
- 建立基础页面布局、SEO 元数据、导航、页脚和响应式容器。
- 定义产品、分类、证据照片、联系方式和下载资料的数据结构。
- 建立三类产品分类与三类买家路径，但不生成虚构 SKU。

验收：

- 本地开发、类型检查和生产构建均可运行。
- 所有已建页面可从导航或页面路径到达，没有空链接。
- 修改一条结构化产品数据可以同步影响产品卡片和详情模板。

### 阶段 2：首页视觉还原

任务：

- 以 B 图为桌面首屏空间依据，还原三栏证据陈列室。
- 使用明确标注的概念图和照片占位，保留 Storefront、Warehouse、Product、Nameplate 四个替换位置。
- 建立三项真实能力带和三个产品分类入口。
- 在首屏之后加入 C 的买家路径与 RFQ 工作区。
- 制作移动端顺序：身份与经营经历 → 主产品 → 真实证据 → 能力 → 产品 → RFQ。

验收：

- 1536 × 1024 桌面截图与批准的 B 图进行同尺寸对照。
- 手机宽度下不存在横向溢出，主行动、表单标签和证据说明可读。
- 所有概念图都带有不会误认成真实照片的标识。

### 阶段 3：核心页面与真实内容边界

本地实现状态（2026-09-13）：已完成并由用户验收。当前版本已生成产品分类、三类合作对象、About、Contact、Privacy、Thank You 和 404 页面；完整 RFQ 后端仍严格保留到阶段 4。

任务：

- 完成产品分类、合作对象、About、Contact、Privacy、Thank You 和 404 页面。
- 产品分类页解释选择该类风机时需要确认的信息，不展示未经核实的具体性能承诺。
- 经销商页面优先呈现产品覆盖、跨品牌采购、包装与物流协调流程。
- 对所有缺失内容使用内部替换清单，不用营销话术掩盖资料缺口。

验收：

- 主导航不包含没有实质内容的页面。
- 全站搜索不到虚构年份、认证、客户、出口国家数量、产能或制造商表述。
- 每个关键页面都有一个明确的下一步：查看产品、联系或提交 RFQ。

### 阶段 4：RFQ 完整流程

实现状态（2026-09-15）：代码、本地模拟流程、独立生产配置、Worker Runtime Secrets 与 Workers Builds 部署已完成。前后端校验、条件字段、同源限制、32 KB 请求上限、Turnstile Siteverify、重复点击锁定、错误关联、邮件分组格式、安全日志和成功页会话确认均已实现。正式域名 `https://xingxufan.com` 已公开运行，`www` 跳转及 Email Routing 已由用户完成。历史提交 `e1eee00` 的首次构建部署在没有公开入口时完成隐藏验证，不再代表当前公开部署状态。当前 RFQ 已通过校验与 Turnstile，但在 `RFQ_EMAIL.send(...)` 步骤返回 `delivery_failed`；生产故障尚未修复，真实 Gmail 收件仍待验收。

本次安全诊断补丁接收邮件发送异常，只记录白名单错误码、固定分类及有效整数 HTTP 状态，不记录原始错误消息、堆栈、邮箱、询盘、Token 或 Secret。未知异常安全降级，前端通用失败响应不变。自动化测试显式注入模拟运行时并禁用远程绑定，不读取 Wrangler 配置或 `.dev.vars`。补丁发布后先确认部署提交，再由用户提供新的测试请求参考编号，按明确错误码定位；本地测试与构建成功不证明真实投递成功。

任务：

- 表单包含客户类型、姓名、公司、国家或地区、Email／WhatsApp、产品类别或型号、数量、目的地、需求说明和隐私确认。
- 根据 `Need selection help` 显示风量、压力、电气条件、尺寸、材质、工况等条件字段。
- 实现前端与服务器双重校验、提交中防重复、成功和失败状态，以及填写内容保留。
- 在服务器端验证 Turnstile，再发送到已验证 Gmail。
- 邮件正文按字段分组，明确来源页面和提交时间；不在日志中复制完整询盘正文。
- `local-test` 只接受 localhost hostname 列表与 Cloudflare 测试响应；`live` 模式拒绝 localhost、`.test` 和 `.invalid` 配置。

验收：

- 正常询盘能够真实到达指定 Gmail。（待修复邮件发送故障并完成真实收件验收）
- 无效字段、过期或重复 Turnstile token、邮件发送失败都有可理解的页面反馈。
- 键盘可以完成整张表单，错误信息与字段正确关联。

### 阶段 5：真实素材替换

状态：用户已明确延期。当前概念图与占位图必须继续显著标注，不得冒充真实照片或参数证据。

任务：

- 接收并分类门店、仓库、产品和铭牌原图，原始文件保持不变。
- 为网页生成优化副本、缩略图和替代文本。
- 从铭牌和资料表转录参数，并由用户或公司人员复核后发布。
- 填入经过逐项授权的 Facebook、WhatsApp、Gmail、WeChat 和地址信息。

验收：

- 每张上线图片都能追溯到真实来源，不使用概念图冒充实拍。
- 关键产品参数与照片／铭牌资料一致。
- 所有联系方式逐项测试，移动端能打开对应渠道。

### 阶段 6：质量、SEO 与安全检查

任务：

- 执行类型检查、生产构建、链接检查、表单测试和基本自动化测试。
- 检查桌面与手机布局、键盘操作、焦点、颜色对比、表单错误和减少动画偏好。
- 设置页面标题、描述、规范链接、Open Graph、站点地图、robots、结构化公司与产品数据；只写可证实内容。
- 检查资源体积、图片尺寸、缓存行为和主要加载体验。
- 对实现页面与 B 图进行最终视觉对照，并记录仍由占位内容造成的差异。

验收：

- 构建和测试通过，内部链接没有 404。
- 没有密钥进入前端包、仓库或浏览器控制台。
- 首页在慢速网络与常见手机宽度下仍能完成产品浏览和询价。

### 阶段 7：预览、仓库与 Cloudflare 交付

实现状态（2026-09-15）：GitHub／Cloudflare Workers Builds 的跨平台 CI 入口、安全配置生成、秘密分层、失败关闭、输出脱敏和清理测试已完成并提交到 `main`，GitHub App 授权与目标仓库连接也已完成。历史提交 `e1eee00` 的首次 Build 与 Deploy 均成功且报告 `No targets deployed`；之后用户已完成正式域名公开部署、`www` 跳转与 Email Routing。根配置与 production 环境继续关闭 `workers.dev` 和 Preview URL，仓库不声明 Route；当前正式域名与必要路由由控制台另行管理，不再处于域名未绑定状态。普通 `main` 推送会按现有连接触发自动构建部署；真实 RFQ 收件和后续质量检查尚未完成。

任务：

- 本地 Preview 与首次 Workers Builds 隐藏验证已完成；正式域名现已公开运行，排查邮件故障无需另行开启公开 Preview。
- 用户确认后才选择性暂存、提交并推送到新 GitHub 仓库。（初次 Git 发布及 CI 适配提交已完成。）
- 通过 Cloudflare Workers Builds 连接该仓库。（已连接；首次隐藏构建与部署已经完成并通过核验。）
- 正式域名部署、Turnstile widget、Runtime Secrets 与 Build variables 已由用户完成；发件身份／邮件绑定的真实投递能力仍须验收，后续 DNS、域名／Route、绑定或秘密值变更需要单独授权。
- 上线后完成一次真实访问、真实表单投递、404、移动端和联系方式检查。

验收：

- Preview 与最终提交对应，仓库工作区干净，`main` 与远端同步。
- 正式域名使用 HTTPS，RFQ 能真实送达且密钥只存在于 Cloudflare 配置中。
- 未解决的内容占位不会被当作正式网站发布。

## 5. 开发前默认决定

如用户没有另行修改，开发时采用以下默认值：

- 使用 B 的首屏，C 的 RFQ 结构位于首屏之后和独立 RFQ 页面。
- 第一版 English only。
- 第一版无 CMS、无账号、无购物车、无询盘数据库、无文件上传。
- 产品详情页只在真实资料达到发布标准时生成。
- Applications 与 Resources 没有真实内容时不进入主导航。
- 概念图可用于本地开发预览，但正式上线前必须替换或保留显著的概念标识。

## 6. 开发开始后仍需用户提供的资料

网站早期版本已经公开运行；下列未补齐资料仍影响真实内容替换和完整质量验收，不能用概念图或虚构信息填补：

- 公开使用的 Facebook 页面 URL。
- WhatsApp 号码及国际格式。
- Gmail 地址。
- WeChat ID 或二维码是否公开。
- 可公开的门店地址与地图策略。
- 门店、仓库、产品和铭牌原图。
- 首批真实型号、参数、包装、MOQ、交期和质保政策。
- `xingxufan.com` 已公开运行，`www` 跳转已由用户完成；以后域名／Route、DNS 或邮件配置变更仍需单独授权。

## 7. 已确认与当前停止边界

阶段 0～3 已完成。阶段 4 的本地实现、生产配置、GitHub／Workers Builds 连接与正式域名部署已完成，`www` 跳转与 Email Routing 已由用户确认完成；RFQ 已通过 Turnstile，但邮件发送仍失败。当前最高优先级是通过安全诊断日志查明明确根因，并完成一次真实 Gmail 收件验收。用户已授权安全诊断补丁与本次选择性 Git 发布；推送 `main` 按既有连接触发自动构建部署，不自动授权修改 Cloudflare DNS、Secrets、绑定、邮件权限或域名／Route。真实素材替换已延期，质量检查与最终隐私文本仍待验收。不得要求用户在聊天中粘贴任何 Secret、真实目标邮箱或完整客户询盘。
