# Cloudflare's public, always-pass Turnstile test sitekey.
# Local builds may use this public test value.
# Cloudflare Workers Builds must set the real value as a Build variable named
# PUBLIC_TURNSTILE_SITE_KEY; the CI build rejects this test key.
PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
